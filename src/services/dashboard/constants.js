// --[ API Endpoints ]---------------------------------------------------

import { BASE_API } from '../constants'

const DASHBOARD = {
  'all': `${BASE_API}user/dashboards/`,
  'new': `${BASE_API}user/dashboards/new`,
}

export {
  DASHBOARD
}
